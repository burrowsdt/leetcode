// 389. Find The Difference
// You are given two strings s and t.

// String t is generated by random shuffling string s and then add one more letter at a random position.

// Return the letter that was added to t.

/**
 * @param {string} s
 * @param {string} t
 * @return {character}
 */

// Solution #1 - basic for loop, pretty good performance time, could be better in memory?
// Runtime: 88 ms, faster than 64.88% of JavaScript online submissions for Find the Difference.
// Memory Usage: 47.3 MB, less than 5.37% of JavaScript online submissions for Find the Difference.

var findTheDifference = function(s, t) {
    let originalArray = s.split("").sort()
    console.log(originalArray)
    let newArray = t.split("").sort()
    console.log(newArray)
    for (let i = 0; i < newArray.length; i++) {
        if (newArray[i] === originalArray[i]) {
            continue
        } else {
            return newArray[i]
        }
    }
}

// Example 1
console.log(findTheDifference(s = "abcd", t = "abcde"))
console.log(findTheDifference(s = "", t = "y"))